import { Authenticationformvalid } from './authenticationformvalid';

describe('Authenticationformvalid', () => {
  it('should create an instance', () => {
    expect(new Authenticationformvalid()).toBeTruthy();
  });
});
